<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Campeonato Público - LIGA GOIANA DE FUTEBOL DE BASE</title>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

<style>
:root{
  --bg:#061427;
  --card:#071d37;
  --card2:#0d2a52;
  --gold:#f7c600;
  --gold2:#ffe45c;
  --white:#ffffff;
  --muted:#cbd6ea;
  --line:rgba(255,255,255,.12);
  --gold-line:rgba(247,198,0,.45);
  --green:#1b5e20;
  --yellow:#7a5a00;
  --red:#b42323;
  --blue:#1a3f73;
  --shadow:0 14px 34px rgba(0,0,0,.38);
  --max:1240px;
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}

html{
  scroll-behavior:smooth;
}

body{
  font-family:Arial, sans-serif;
  background:
    linear-gradient(rgba(2,8,20,.88), rgba(2,8,20,.96)),
    url("fundo.png") center/cover fixed no-repeat;
  color:var(--white);
  min-height:100vh;
  overflow-x:hidden;
}

a{
  text-decoration:none;
  color:inherit;
}

button{
  font-family:inherit;
  cursor:pointer;
  border:none;
}

img{
  display:block;
  max-width:100%;
}

.wrap{
  width:min(100% - 28px, var(--max));
  margin:0 auto;
}

/* HEADER PADRÃO */
.site-header{
  background:linear-gradient(180deg, rgba(2,10,24,.98), rgba(8,23,44,.96));
  border-bottom:3px solid var(--gold);
  box-shadow:var(--shadow);
}

.header-top{
  min-height:92px;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:18px 0;
}

.header-title{
  text-align:center;
  font-size:26px;
  font-weight:900;
  letter-spacing:.8px;
  text-transform:uppercase;
  line-height:1.15;
}

/* MENU FINAL PADRÃO */
.nav-bar{
  background:linear-gradient(180deg, rgba(16,42,82,.96), rgba(8,23,44,.98));
  border-top:1px solid rgba(255,255,255,.06);
  overflow:hidden;
}

.menu{
  display:flex;
  justify-content:center;
  align-items:center;
  gap:clamp(4px, .55vw, 10px);
  padding:14px 6px;
  flex-wrap:nowrap;
  overflow:hidden;
}

.menu a{
  color:#fff;
  font-weight:900;
  font-size:clamp(11px, .92vw, 15px);
  background:linear-gradient(180deg, rgba(16,42,82,.98), rgba(12,33,64,.98));
  padding:clamp(8px, .75vw, 12px) clamp(9px, 1vw, 18px);
  border-radius:clamp(10px, .9vw, 15px);
  border:1px solid rgba(255,255,255,.07);
  white-space:nowrap;
  flex:0 1 auto;
  box-shadow:0 10px 22px rgba(0,0,0,.24);
  transition:.25s ease;
}

.menu a:hover{
  color:var(--gold);
  border-color:var(--gold-line);
}

.menu a.ativo{
  background:linear-gradient(135deg, var(--gold), var(--gold2));
  color:#111;
}
/* CONTAINER */
.container{
  padding:26px 0 36px;
}

/* HERO */
.hero{
  background:linear-gradient(135deg, #0d2a52, #071d37);
  border:1px solid var(--gold-line);
  border-radius:22px;
  padding:20px;
  margin-bottom:18px;
  box-shadow:var(--shadow);
}

.hero-top{
  display:flex;
  align-items:center;
  gap:16px;
  flex-wrap:wrap;
}

.hero-logo{
  width:80px;
  height:80px;
  border-radius:18px;
  object-fit:cover;
  border:2px solid var(--gold);
  background:#fff;
}

.hero-info{
  flex:1;
}

.hero h2{
  color:var(--gold);
  font-size:24px;
  font-weight:900;
  margin-bottom:6px;
}

.hero p{
  color:var(--muted);
  font-size:14px;
}

.hero-tags{
  display:flex;
  gap:6px;
  flex-wrap:wrap;
  margin-top:10px;
}

.hero-tag{
  background:#0b2447;
  padding:6px 10px;
  border-radius:999px;
  font-size:12px;
  font-weight:800;
}

/* BOTÕES */
.actions{
  display:flex;
  gap:8px;
  flex-wrap:wrap;
  margin-top:12px;
}

.btn{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:6px;
  padding:10px 12px;
  border-radius:10px;
  font-weight:900;
  font-size:12px;
}

.btn-main{
  background:var(--gold);
  color:#111;
}

.btn-sec{
  background:#1a3f73;
  color:#fff;
}
/* GRID PRINCIPAL */
.grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:14px;
  margin-bottom:14px;
}

/* CARD PADRÃO */
.card{
  background:linear-gradient(180deg, #0d2a52, #071d37);
  border-radius:18px;
  padding:16px;
  border:1px solid rgba(255,255,255,.08);
  box-shadow:var(--shadow);
}

.card h3{
  color:var(--gold);
  font-size:18px;
  margin-bottom:12px;
  font-weight:900;
}

/* INFO */
.info-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:10px;
}

.info{
  background:#081c36;
  border-radius:12px;
  padding:10px;
}

.info strong{
  display:block;
  font-size:11px;
  color:var(--gold);
  margin-bottom:4px;
}

.info span{
  font-size:13px;
  font-weight:800;
}

/* TAGS */
.tags{
  display:flex;
  flex-wrap:wrap;
  gap:6px;
  margin-top:10px;
}

.tag{
  background:#1a3f73;
  padding:5px 9px;
  border-radius:999px;
  font-size:11px;
}

/* REGRAS */
.rules{
  display:flex;
  flex-direction:column;
  gap:8px;
}

.rule{
  background:#081c36;
  padding:10px;
  border-radius:12px;
  font-size:13px;
}

/* JOGOS */
.games{
  display:flex;
  flex-direction:column;
  gap:10px;
}

.game{
  background:#081c36;
  border-radius:12px;
  padding:10px;
}

.game strong{
  font-size:14px;
}

.game small{
  display:block;
  font-size:12px;
  color:var(--muted);
}

/* STATUS */
.status{
  margin-top:6px;
  display:inline-block;
  padding:5px 8px;
  border-radius:999px;
  font-size:11px;
  font-weight:900;
}

.final{
  background:var(--green);
}

.agendado{
  background:var(--yellow);
}
/* RESPONSIVO */
@media(max-width:900px){
  .grid{
    grid-template-columns:1fr;
  }

  .info-grid{
    grid-template-columns:1fr;
  }
}

@media(max-width:600px){
  .wrap{
    width:min(100% - 18px, var(--max));
  }

  .header-left,
  .header-right{
    display:none;
  }

  .header-title{
    font-size:16px;
    text-align:center;
  }

  .menu{
    justify-content:flex-start;
    padding:10px;
  }

  .hero{
    padding:14px;
  }

  .hero h2{
    font-size:20px;
  }

  .hero-logo{
    width:70px;
    height:70px;
  }

  .actions{
    flex-direction:column;
  }

  .btn{
    width:100%;
  }
}
</style>
</head>

<body>

<header>
  <div class="wrap header-top">

    <div class="header-left">
      <button class="top-btn btn-light" onclick="voltarPagina()">
        <i class="fa-solid fa-arrow-left"></i> Voltar
      </button>

      <a href="index.html" class="top-btn btn-light">
        <i class="fa-solid fa-house"></i> Página Inicial
      </a>
    </div>

    <div class="header-title">
      LIGA GOIANA DE FUTEBOL DE BASE
    </div>

    <div class="header-right"></div>
  </div>

  <div class="nav-bar">
    <div class="wrap">
      <nav class="menu">
        <a href="index.html">Início</a>
        <a href="campeonatos.html" class="ativo">Campeonatos</a>
        <a href="tabela.html">Tabela</a>
        <a href="jogos.html">Jogos</a>
        <a href="times.html">Times</a>
        <a href="tecnicos.html">Técnicos</a>
        <a href="jogadores.html">Jogadores</a>
        <a href="arbitros.html">Árbitros</a>
        <a href="assistentes.html">Assistente</a>
        <a href="sumula-publica.html">Súmula</a>
        <a href="noticias.html">Notícias</a>
        <a href="login.html">Login</a>
      </nav>
    </div>
  </div>
</header>

<main class="wrap container" id="conteudo" style="display:none;">

  <!-- HERO -->
  <section class="hero">
    <div class="hero-top">

      <img id="logoCampeonato" class="hero-logo" src="logo-liga.jfif">

      <div>
        <h2 id="nomeCampeonato">Carregando...</h2>
        <p id="descricaoCampeonato">Carregando informações...</p>

        <div class="hero-tags">
          <div class="hero-tag" id="tagTemporada">Temporada</div>
          <div class="hero-tag" id="tagStatus">Status</div>
          <div class="hero-tag" id="tagCategorias">Categorias</div>
          <div class="hero-tag" id="tagJogos">Jogos</div>
          <div class="hero-tag" id="tagTimes">Times</div>
          <div class="hero-tag" id="tagGols">Gols</div>
        </div>

        <div class="actions">
          <a id="btnTabela" class="btn btn-gold">
            <i class="fa-solid fa-table"></i> Tabela
          </a>

          <a id="btnJogos" class="btn btn-blue">
            <i class="fa-solid fa-calendar"></i> Jogos
          </a>

          <a id="btnTimes" class="btn btn-blue">
            <i class="fa-solid fa-shield"></i> Times
          </a>
        </div>
      </div>

    </div>
  </section>
<!-- GRID PRINCIPAL -->
  <section class="grid">

    <!-- RESUMO -->
    <div class="card">
      <h3>Resumo do Campeonato</h3>

      <div class="info-grid">
        <div class="info-item">
          <strong>Nome</strong>
          <span id="infoNome">-</span>
        </div>

        <div class="info-item">
          <strong>Temporada</strong>
          <span id="infoTemporada">-</span>
        </div>

        <div class="info-item">
          <strong>Status</strong>
          <span id="infoStatus">-</span>
        </div>

        <div class="info-item">
          <strong>Tipo</strong>
          <span id="infoTipo">-</span>
        </div>

        <div class="info-item">
          <strong>Início</strong>
          <span id="infoInicio">-</span>
        </div>

        <div class="info-item">
          <strong>Fim</strong>
          <span id="infoFim">-</span>
        </div>
      </div>

      <div style="margin-top:15px;">
        <strong style="color:var(--gold);">Descrição</strong>
        <p id="descricaoCompleta">-</p>
      </div>
    </div>

    <!-- ESTATÍSTICAS -->
    <div class="card">
      <h3>Informações Gerais</h3>

      <div class="stats-grid">
        <div class="stat-card"><strong>Categorias</strong><span id="totalCategorias">0</span></div>
        <div class="stat-card"><strong>Jogos</strong><span id="totalJogos">0</span></div>
        <div class="stat-card"><strong>Finalizados</strong><span id="totalFinalizados">0</span></div>
        <div class="stat-card"><strong>Times</strong><span id="totalTimes">0</span></div>
        <div class="stat-card"><strong>Jogadores</strong><span id="totalJogadores">0</span></div>
        <div class="stat-card"><strong>Técnicos</strong><span id="totalTecnicos">0</span></div>
        <div class="stat-card"><strong>Gols</strong><span id="totalGols">0</span></div>
      </div>

      <div style="margin-top:15px;">
        <strong style="color:var(--gold);">Categorias</strong>
        <div class="tags" id="listaCategorias"></div>
      </div>
    </div>

  </section>
<!-- REGRAS -->
  <section class="card" style="margin-bottom:15px;">
    <h3>Regras Disciplinares</h3>

    <div id="listaRegras" style="display:flex;flex-direction:column;gap:10px;"></div>
  </section>

  <!-- JOGOS -->
  <section class="card" style="margin-bottom:15px;">
    <h3>Próximos Jogos / Resultados</h3>

    <div id="listaJogos" style="display:flex;flex-direction:column;gap:10px;"></div>
  </section>

  <!-- REGULAMENTO -->
  <section class="card">
    <h3>Regulamento</h3>
    <p id="textoRegulamento">-</p>
  </section>

</main>

<!-- LOADING -->
<div class="wrap" id="loading">
  <div class="card" style="text-align:center;">Carregando campeonato...</div>
</div>

<!-- ERRO -->
<div class="wrap" id="erro" style="display:none;">
  <div class="card" style="text-align:center;">Campeonato não encontrado.</div>
</div>
<script type="module">
import { db } from "./firebase.js";
import {
  doc,
  getDoc,
  collection,
  getDocs
} from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";

const conteudo = document.getElementById("conteudo");
const loading = document.getElementById("loading");
const erro = document.getElementById("erro");

let campeonato = null;
let jogos = [];
let jogadores = [];
let tecnicos = [];
let sumulas = [];

/* VOLTAR */
window.voltarPagina = function(){
  if(document.referrer){
    history.back();
  }else{
    location.href = "campeonatos.html";
  }
};

/* UTIL */
function param(nome){
  return new URLSearchParams(location.search).get(nome);
}

function normalizar(txt){
  return String(txt||"")
    .toLowerCase()
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g,"");
}

function val(obj, chaves, f=""){
  for(const c of chaves){
    if(obj && obj[c]!==undefined && obj[c]!==null && obj[c]!== ""){
      return obj[c];
    }
  }
  return f;
}
function nomeCamp(c){
  return val(c, ["nome", "campeonato", "titulo"], "");
}

function idCamp(c){
  return val(c, ["id", "campeonatoId"], "");
}

function dataBR(valor){
  if(!valor) return "-";

  const d = new Date(valor);

  if(isNaN(d.getTime())){
    return String(valor);
  }

  return d.toLocaleString("pt-BR", {
    day:"2-digit",
    month:"2-digit",
    year:"numeric",
    hour:"2-digit",
    minute:"2-digit"
  });
}

/* DADOS EXEMPLO */
const campeonatoExemplo = {
  id:"copa-mundo-2002",
  nome:"Copa do Mundo FIFA 2002",
  temporada:"2002",
  status:"Finalizado",
  tipo:"Mata-mata",
  logo:"logo-liga.jfif",
  descricao:"Campeonato de exemplo com a final Brasil 2 x 0 Alemanha para demonstrar o funcionamento do sistema.",
  regulamento:"Exemplo de regulamento da competição. Os dados podem ser removidos depois.",
  categorias:["Seleção Principal"],
  publicado:true,
  publico:true,
  exemplo:true,
  regrasCartoes:{
    amarelosParaSuspensao:2,
    jogosSuspensaoAmarelos:1,
    jogosSuspensaoVermelhoDireto:1,
    jogosSuspensaoSegundoAmarelo:1
  }
};

const jogosExemplo = [
  {
    id:"final-copa-2002",
    campeonatoId:"copa-mundo-2002",
    campeonato:"Copa do Mundo FIFA 2002",
    categoria:"Seleção Principal",
    rodada:"Final",
    data:"2002-06-30T08:00:00",
    local:"International Stadium Yokohama",
    timeA:"Brasil",
    timeB:"Alemanha",
    golsA:2,
    golsB:0,
    status:"finalizado",
    exemplo:true
  }
];

const jogadoresExemplo = [
  { id:"ronaldo-2002", nome:"Ronaldo", time:"Brasil", exemplo:true },
  { id:"rivaldo-2002", nome:"Rivaldo", time:"Brasil", exemplo:true },
  { id:"kahn-2002", nome:"Oliver Kahn", time:"Alemanha", exemplo:true }
];

const tecnicosExemplo = [
  { id:"scolari-2002", nome:"Luiz Felipe Scolari", time:"Brasil", exemplo:true },
  { id:"voller-2002", nome:"Rudi Völler", time:"Alemanha", exemplo:true }
];

const sumulasExemplo = [
  {
    id:"final-copa-2002",
    jogoId:"final-copa-2002",
    campeonatoId:"copa-mundo-2002",
    campeonato:"Copa do Mundo FIFA 2002",
    categoria:"Seleção Principal",
    timeA:"Brasil",
    timeB:"Alemanha",
    golsA:2,
    golsB:0,
    status:"finalizada",
    exemplo:true
  }
];

function pertenceAoCamp(item){
  const campId = idCamp(campeonato);
  const campNome = nomeCamp(campeonato);

  const itemCampId = val(item, ["campeonatoId"], "");
  const itemCampNome = val(item, ["campeonato", "nomeCampeonato"], "");

  return itemCampId === campId ||
    normalizar(itemCampNome) === normalizar(campNome);
}

function jogosCamp(){
  return jogos.filter(pertenceAoCamp);
}

function sumulasCamp(){
  return sumulas.filter(pertenceAoCamp);
}

function calcularTotais(){
  const listaJogos = jogosCamp();
  const listaSumulas = sumulasCamp();

  const categorias = new Set();
  const times = new Set();

  listaJogos.forEach(j => {
    if(j.categoria) categorias.add(j.categoria);
    if(j.timeA) times.add(normalizar(j.timeA));
    if(j.timeB) times.add(normalizar(j.timeB));
  });

  const nomesTimes = Array.from(times);

  const jogadoresDoCamp = jogadores.filter(j =>
    nomesTimes.includes(normalizar(j.time))
  );

  const tecnicosDoCamp = tecnicos.filter(t =>
    nomesTimes.includes(normalizar(t.time))
  );

  let gols = 0;

  listaSumulas.forEach(s => {
    gols += Number(s.golsA || 0);
    gols += Number(s.golsB || 0);
  });

  if(gols === 0){
    listaJogos.forEach(j => {
      gols += Number(j.golsA || 0);
      gols += Number(j.golsB || 0);
    });
  }

  return {
    jogos:listaJogos,
    sumulas:listaSumulas,
    categorias:categorias.size || (Array.isArray(campeonato.categorias) ? campeonato.categorias.length : 0),
    times:times.size,
    jogadores:jogadoresDoCamp.length,
    tecnicos:tecnicosDoCamp.length,
    gols
  };
}
function preencherJogos(listaJogos){
  const box = document.getElementById("listaJogos");

  if(!listaJogos.length){
    box.innerHTML = `<div class="vazio">Nenhum jogo cadastrado para este campeonato.</div>`;
    return;
  }

  const ordenados = [...listaJogos].sort((a,b) =>
    String(a.data || "").localeCompare(String(b.data || ""))
  );

  box.innerHTML = ordenados.slice(0,8).map(j => {
    const finalizado = normalizar(j.status) === "finalizado" || normalizar(j.status) === "sumula_enviada";
    const placar = finalizado ? `${Number(j.golsA || 0)} x ${Number(j.golsB || 0)}` : "x";

    return `
      <div class="game">
        <strong>${j.timeA || "-"} ${placar} ${j.timeB || "-"}</strong>
        <small>Categoria: ${j.categoria || "-"}</small>
        <small>Data: ${dataBR(j.data)}</small>
        <small>Local: ${j.local || "-"}</small>

        <div class="status ${finalizado ? "final" : "agendado"}">
          ${finalizado ? "Finalizado" : "Agendado"}
        </div>
      </div>
    `;
  }).join("");
}

function preencherTela(){
  const totais = calcularTotais();
  const nome = nomeCamp(campeonato);

  document.title = `${nome || "Campeonato"} - LIGA GOIANA DE FUTEBOL DE BASE`;

  document.getElementById("logoCampeonato").src = campeonato.logo || "logo-liga.jfif";
  document.getElementById("logoCampeonato").onerror = function(){ this.src = "logo-liga.jfif"; };

  document.getElementById("nomeCampeonato").textContent = nome || "Campeonato";
  document.getElementById("descricaoCampeonato").textContent =
    campeonato.descricao || campeonato.resumo || "Sem descrição cadastrada.";

  document.getElementById("tagTemporada").textContent = `Temporada: ${campeonato.temporada || "-"}`;
  document.getElementById("tagStatus").textContent = `Status: ${campeonato.status || "-"}`;
  document.getElementById("tagCategorias").textContent = `Categorias: ${totais.categorias}`;
  document.getElementById("tagJogos").textContent = `Jogos: ${totais.jogos.length}`;
  document.getElementById("tagTimes").textContent = `Times: ${totais.times}`;
  document.getElementById("tagGols").textContent = `Gols: ${totais.gols}`;

  document.getElementById("infoNome").textContent = nome || "-";
  document.getElementById("infoTemporada").textContent = campeonato.temporada || "-";
  document.getElementById("infoStatus").textContent = campeonato.status || "-";
  document.getElementById("infoTipo").textContent = campeonato.tipo || campeonato.formato || "-";
  document.getElementById("infoInicio").textContent = campeonato.dataInicio || campeonato.inicio || "-";
  document.getElementById("infoFim").textContent = campeonato.dataFim || campeonato.fim || "-";
  document.getElementById("descricaoCompleta").textContent =
    campeonato.descricao || campeonato.resumo || "Sem descrição cadastrada.";

  document.getElementById("totalCategorias").textContent = totais.categorias;
  document.getElementById("totalJogos").textContent = totais.jogos.length;
  document.getElementById("totalFinalizados").textContent = totais.sumulas.length;
  document.getElementById("totalTimes").textContent = totais.times;
  document.getElementById("totalJogadores").textContent = totais.jogadores;
  document.getElementById("totalTecnicos").textContent = totais.tecnicos;
  document.getElementById("totalGols").textContent = totais.gols;

  const categorias = Array.isArray(campeonato.categorias) ? campeonato.categorias : [];

  document.getElementById("listaCategorias").innerHTML = categorias.length
    ? categorias.map(c => `<span class="tag">${typeof c === "string" ? c : (c.nome || c.categoria || "")}</span>`).join("")
    : `<span class="tag">Sem categorias</span>`;

  const regras = campeonato.regrasCartoes || {};

  document.getElementById("listaRegras").innerHTML = `
    <div class="rule">
      <strong style="color:var(--gold);">Suspensão por amarelos</strong><br>
      ${Number(regras.amarelosParaSuspensao || 3)} amarelos = ${Number(regras.jogosSuspensaoAmarelos || 1)} jogo(s) de suspensão
    </div>

    <div class="rule">
      <strong style="color:var(--gold);">Vermelho direto</strong><br>
      Vermelho direto = ${Number(regras.jogosSuspensaoVermelhoDireto || 2)} jogo(s) de suspensão
    </div>

    <div class="rule">
      <strong style="color:var(--gold);">Segundo amarelo</strong><br>
      2º amarelo = ${Number(regras.jogosSuspensaoSegundoAmarelo || 1)} jogo(s) de suspensão
    </div>
  `;

  document.getElementById("textoRegulamento").textContent =
    campeonato.regulamento || campeonato.regulamentoTexto || campeonato.observacoes || "Sem regulamento cadastrado.";

  document.getElementById("btnTabela").href = `tabela.html?campeonato=${encodeURIComponent(nome)}`;
  document.getElementById("btnJogos").href = `jogos.html?campeonato=${encodeURIComponent(nome)}`;
  document.getElementById("btnTimes").href = `times.html?campeonato=${encodeURIComponent(nome)}`;

  preencherJogos(totais.jogos);
}

async function carregarRelacionados(){
  const [snapJogos, snapJogadores, snapTecnicos, snapSumulas] = await Promise.all([
    getDocs(collection(db, "jogos")),
    getDocs(collection(db, "jogadores")),
    getDocs(collection(db, "tecnicos")),
    getDocs(collection(db, "sumulas"))
  ]);

  jogos = [];
  jogadores = [];
  tecnicos = [];
  sumulas = [];

  snapJogos.forEach(d => jogos.push({ id:d.id, ...d.data() }));
  snapJogadores.forEach(d => jogadores.push({ id:d.id, ...d.data() }));
  snapTecnicos.forEach(d => tecnicos.push({ id:d.id, ...d.data() }));
  snapSumulas.forEach(d => sumulas.push({ id:d.id, ...d.data() }));
}

async function iniciar(){
  try{
    const id = param("id");

    if(id === "copa-mundo-2002"){
      campeonato = campeonatoExemplo;
      jogos = jogosExemplo;
      jogadores = jogadoresExemplo;
      tecnicos = tecnicosExemplo;
      sumulas = sumulasExemplo;
      preencherTela();
      loading.style.display = "none";
      conteudo.style.display = "block";
      return;
    }

    if(!id){
      campeonato = campeonatoExemplo;
      jogos = jogosExemplo;
      jogadores = jogadoresExemplo;
      tecnicos = tecnicosExemplo;
      sumulas = sumulasExemplo;
      preencherTela();
      loading.style.display = "none";
      conteudo.style.display = "block";
      return;
    }

    const snap = await getDoc(doc(db, "campeonatos", id));

    if(!snap.exists()){
      throw new Error("Campeonato não encontrado");
    }

    campeonato = { id:snap.id, ...snap.data() };

    await carregarRelacionados();

    preencherTela();

    loading.style.display = "none";
    conteudo.style.display = "block";
  }catch(e){
    console.error(e);
    loading.style.display = "none";
    erro.style.display = "block";
  }
}

iniciar();
</script>

</body>
</html>
